[toc]

# 项目

1. 拖拽课表到具体哪一节课
2. vite 项目上传文件到 阿里云, 但是阿里云的 ali-oss JDK, 是依赖于node环境的, 在vite里运行, 如果是开发环境,可以正常运行, 如果是生产环境, 那就没办法了, 没有浏览器环境中运行不了这个SDK, 所以使用node进行了一次转发, 在服务器上运行一个node服务, 然后 vite 项目调取node的接口, node去上传这个文件, 并把结果返回回来, 这样就可以了

